import { PlaySoundOnClick } from './play-sound-on-click';

describe('PlaySoundOnClick', () => {
  it('should create an instance', () => {
    const directive = new PlaySoundOnClick();
    expect(directive).toBeTruthy();
  });
});
